package com.techvision.common;

import java.util.List;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class TestClass {

	public static void main(String[] args) throws Exception {/*
		
		//new SimpleDateFormat("dd/MM/yyyy").parse(value.toString());
		Pattern regex = Pattern.compile("[a-z][\s\S]*>");
		Matcher matcher = regex.matcher(templateContent);
		while(matcher.find()) {
			String[] splitArray =  matcher.group(1).split(",");
			Patt
			if(splitArray[0].)
			documentParameterList.add(new  DocumentParameter(splitArray[0], splitArray[1]));
		}
	
	/<[a-z][\s\S]*>/i.test()
		
	*/}

}
